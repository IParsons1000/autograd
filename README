<!--- vim: syntax=markdown
-->

autograd
========

a lightweight autograd package

Building
--------

To build the autograd package, run

```bash
python3 -m build
```

To optionally test the build, run

```bash
pytest
```

Installation
------------

To install autograd on your system, run

```bash
pip install .
```